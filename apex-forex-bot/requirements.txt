# Pinned to the versions currently running in production, read from the live
# Render build log — so these are known to resolve and install on the
# deployment's Python, not guessed.
#
# Why exact pins and not ">=": Render runs `pip install -r requirements.txt`
# on every build, so a floor alone means any upstream release ships itself
# straight into a live trading bot with no review. Twice in one day a
# transitive upgrade broke this project's own tooling. Reproducible builds are
# the point — bump these deliberately, not by accident.
#
# Every entry below is imported at runtime; the list was checked against the
# actual import graph. The `groq` SDK used to be here and was never imported:
# the Groq calls go out over plain `requests`.

# The HTTP client for everything that is not the broker socket: the cTrader
# OAuth token exchange, Telegram, the licence check, market and news feeds.
# Every call site uses the plain module-level API — no Session, no
# HTTPAdapter, no verify=, no proxies=, no custom auth — which is the most
# stable part of the surface.
#
# 2.33.0 is the floor that clears both findings against 2.32.3:
#   PYSEC-2026-1872  .netrc credential leak via malicious URLs   (2.32.4)
#   PYSEC-2026-2275  extract_zipped_paths predictable temp path  (2.33.0)
# 2.34.2 is the current release, clears the same set, and its changes over
# 2.33 are inline typing plus two runtime fixes (non-greedy no_proxy
# matching, duplicate leading slashes in paths — the latter complete only
# with urllib3 >= 2.7.0, and 2.8.0 is what installs). Needs Python >= 3.10;
# matplotlib below already requires >= 3.11, so this raises no floor.
requests==2.34.2
redis==8.1.0
python-dotenv==1.2.2
# Field-level encryption of broker tokens at rest (Fernet, in
# apex/user_store.py — the only thing in this repository that uses this
# package). NOT the broker TLS path: that is stdlib `ssl` against the system
# OpenSSL, so this pin has no bearing on the cTrader connection.
#
# Held at 42.0.8 until 2026-09-26 by `pyOpenSSL==24.1.0`, which required
# `cryptography<43,>=41.0.5` and arrived transitively with
# `ctrader-open-api`. Vendoring the protobuf stubs removed both, so this
# became raisable. 50.0.0 is the floor that clears the last finding
# (PYSEC-2026-3552); 50.0.1 is the current patch on that line and clears
# every advisory. Fernet backwards compatibility is pinned by
# tests/test_fernet_compat.py, which decrypts a token minted under 42.0.8.
cryptography==50.0.1
yfinance==1.6.0
# cTrader's protobuf message definitions are vendored under
# apex/ctrader_proto/ instead of coming from `ctrader-open-api`, which
# hard-pinned protobuf==3.20.1, pyOpenSSL==24.1.0 and Twisted==24.3.0 with
# `==` and had no upgrade path (0.9.2 is the newest release; 0.9.3 was
# yanked). We used only the generated message classes, never its Twisted
# client — which connects with VERIFY_NONE. See apex/ctrader_proto/.
#
# 6.33.5 is the first version with no open advisory against it: 3.20.2
# closes PYSEC-2026-899 but leaves PYSEC-2026-1805 and -1806, and 5.29.5
# still leaves -1805. Verified with pip-audit at each step, and the
# vendored stubs were exercised against 3.20.2 / 3.20.3 / 4.25.8 / 5.29.5 /
# 6.33.5 — all pass. Needs Python >= 3.9; matplotlib below already
# requires >= 3.11, so this raises no floor.
protobuf==6.33.5
matplotlib==3.11.1
